version: '3.8'

services:
  web-calculator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./src:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped